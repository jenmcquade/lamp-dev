propel:
    paths:
        projectDir: .
        schemaDir: .
        outputDir: .
        phpDir: ./module
    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=db;port=3306;dbname=mysql_dev
                user: mysql_dev
                password: mysql_dev
                settings:
                    charset: utf8
                    
